import http from '@/utils/http'

import { SET_AMZ } from './actionType';
import { deprecate } from 'util';
import { dispatch } from 'rxjs/internal/observable/range';

const handelAmz = (name,owner,type,approver,dateTime,des) => {
    return(dispatch) => {}
}